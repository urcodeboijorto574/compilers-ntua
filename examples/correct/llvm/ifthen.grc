fun main() : nothing var a : int; {
  a <- 0;
  if a < 1 then {
    writeInteger(1);
    if a < 2 then writeInteger(2);
    if a = 0 then
      while a < 3 do a <- a + 1;
  } writeInteger(a);
  writeString(" = 123\n");
}
