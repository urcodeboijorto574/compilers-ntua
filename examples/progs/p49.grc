fun g(ref d, main : char; ref c, y : int; i : int[]; a, g, k : int; bar, k : char[][10]) : int
fun n(ref d, y : int[40]) : nothing;
var h, j : char;

{
  i(- (+ g));
  m <- l(- 36, z(), ('\n' - '7'));
  ;
  m <- + '\n';
  ;
}
